[project]
name = "frontend-nicegui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nicegui[highcharts]>=2.5.0",
    "httpx>=0.27.0",
    "pydantic>=2.5.0",
]

[tool.uv]
